function compile(instructions: string[]): number {
    // Code here

    const cases = {
        'MOV': (a, b) => {
            if (/\-?\d+/.test(a)) {
                
            }
        }
    }

    return 0
}
